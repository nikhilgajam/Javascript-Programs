// Template literals

var data = "Data";

// Inside tildes(`) when we use a ${code} inside the curly braces all JS operations will work 

document.write(`Hello ${data} !`);

// We can perform the operations such as multiplication and more
