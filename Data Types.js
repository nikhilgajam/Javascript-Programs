// Data Types

// 1. Numerical Values With Or Without Decimals
var a = 100;
var p = 3.14;

// 2. String (String is collen of letters, numbers, punctuations)
var s = "Hello World";

// 3. Boolean (This can be only true or false)
var b = true;

// 4. Object (Ex: A node in the DOM (Document Object Model))
var topic = document.getElementById("ID");

// 5. Array (It contains more than one value or a list of values)
var arr = [1, 2, 3];
var links = document.getElementsByTagName("a");
document.write(arr[0]);
